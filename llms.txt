# Intra-annual Management Strategy Evaluation (iamse)

`iamse` is an R package for **intra-annual, age-based management
strategy evaluation (MSE)**. It is designed to explore how different
harvest control rules (HCRs) perform when key processes (e.g. growth,
recruitment, fishing mortality) vary within the year and through time.

------------------------------------------------------------------------

## Highlights

1.  **Sub-annual time steps**
    - Explicit intra-annual dynamics (e.g. seasons or quarters) in the
      operating model.
2.  **Optional length-dependent processes**
    - Length-based growth, selectivity, and mortality can be included
      where data allow.
3.  **Stochastic age–length key**
    - Flexible, stochastic age–length conversion to link length-based
      observations to age-based dynamics.
4.  **Stochastic reference points**
    - Simulation-based estimation of reference points (e.g. MSY, F_MSY,
      B_MSY, B_0) using
      [`est.ref.levels.stochastic()`](https://tokami.github.io/iamse/reference/est.ref.levels.stochastic.md).
5.  **Dynamic (time-varying) processes and reference points**
    - Time-varying life-history parameters and reference points to
      reflect changing productivity.

------------------------------------------------------------------------

## Getting started

Once the package is installed and loaded, a typical workflow is:

1.  Prepare life-history and stock data with
    [`check.dat()`](https://tokami.github.io/iamse/reference/check.dat.md).
2.  Set up simulation and noise settings with
    [`check.set()`](https://tokami.github.io/iamse/reference/check.set.md).
3.  Estimate reference points (deterministic or stochastic).
4.  Define one or more HCRs
    (e.g. [`def.hcr.conscat()`](https://tokami.github.io/iamse/reference/def.hcr.conscat.md),
    [`def.hcr.ref()`](https://tokami.github.io/iamse/reference/def.hcr.ref.md),
    [`def.hcr.spict()`](https://tokami.github.io/iamse/reference/def.hcr.spict.md)).
5.  Run the MSE with
    [`run.mse()`](https://tokami.github.io/iamse/reference/run.mse.md).
6.  Summarise and visualise results using the `plotiamse.*()` and
    [`est.metrics()`](https://tokami.github.io/iamse/reference/est.metrics.md)
    functions.

A basic example (mirroring the demo vignette):

``` r
library(iamse)

## Example life-history data
data("stocklist")
dat0 <- stocklist$mlh
dat0$CVlen <- 0.1
dat  <- check.dat(dat0)

## Simulation settings
set <- check.set()
set$nysim   <- 20   # projection years
set$nrep    <- 10   # number of replicates (low for demo)
set$refMethod <- "median"

## Noise structure (simplified)
set$noise$rep$H  <- c(0.1, 0, 1)
set$noise$rep$M  <- c(0.1, 0, 1)
set$noise$rep$R0 <- c(0.1, 0, 1)
set$noise$time$R <- c(0.7, 0.4, 1)
set$noise$rep$F  <- c(0.15, 0, 1)
set$noise$time$I <- c(0.3, 0, 1)
set$noise$time$C <- c(0.1, 0, 1)

## Stochastic reference points
set$refN <- 100
dat <- est.ref.levels.stochastic(dat, set, fmax = 5,
                                 ncores = 2, plot = TRUE)

dat$ref$Blim <- 0.2 * dat$ref$B0

## Define some HCRs
iamse::def.hcr.ref(set. = set)                      # "noF"
iamse::def.hcr.ref(consF = "fmsy", set. = set)      # "refFmsy"
iamse::def.hcr.conscat(set. = set)                  # "conscat"

set$hcr <- c("noF", "refFmsy", "conscat")

## Run MSE (single core for portability)
resMSE <- run.mse(dat, set, ncores = 1, verbose = TRUE)

## Plot time series
par(mfrow = c(3, 1), mar = c(4, 4, 1, 1))
plotiamse.f(dat, set, resMSE)
plotiamse.cw(dat, set, resMSE)
plotiamse.b(dat, set, resMSE)
```

------------------------------------------------------------------------

## Documentation

A **vignette** will be available to provide a more complete introduction
to:

- The structure of the operating model,
- How to define and combine HCRs,
- Options for stochastic reference points and productivity analysis,
- Interpretation of key performance metrics and trade-offs.

Until then, the main help pages are a good starting point:

- [`?check.dat`](https://tokami.github.io/iamse/reference/check.dat.md)
  – prepare and validate life-history / stock input.
- [`?check.set`](https://tokami.github.io/iamse/reference/check.set.md)
  – define simulation and noise settings.
- [`?run.mse`](https://tokami.github.io/iamse/reference/run.mse.md) –
  run the MSE given data, settings, and HCRs.

Function-specific documentation is available via the usual R help
system, e.g.:

``` r
?check.dat
?run.mse
?def.hcr.conscat
?plotiamse.tradeoff
```

------------------------------------------------------------------------

## Package requirements

The package depends on several other R packages. Key ones include:

- [`Rcpp`](https://www.rcpp.org) (and `LinkingTo: Rcpp`),
- `doBy`,
- `future`, `future.apply`,
- `parallel`,
- (optionally) [`spict`](https://github.com/DTUAqua/spict) for
  SPiCT-based HCRs.

For the full list of dependencies, see the `DESCRIPTION` file of the
package.

------------------------------------------------------------------------

## Installing the `iamse` package

You can install the development version of `iamse` from GitHub with:

``` r
# install.packages("remotes")
remotes::install_github("tokami/iamse")
```

Then load it with:

``` r
library(iamse)
```

------------------------------------------------------------------------

## Citation

Please use:

``` r
citation("iamse")
```

to get the recommended citations for the package and associated
methodology.

------------------------------------------------------------------------

## Funding

The development of **iamse** was co-funded by the European Union.

------------------------------------------------------------------------

![Co-funded by the EU
logo](reference/figures/EN_Co-fundedbytheEU_RGB_POS.png)

Co-funded by the EU logo

# Package index

## All functions

- [`advancepop()`](https://tokami.github.io/iamse/reference/advancepop.md)
  : advancepop
- [`baranov()`](https://tokami.github.io/iamse/reference/baranov.md) :
  baranov
- [`check.dat()`](https://tokami.github.io/iamse/reference/check.dat.md)
  : check.dat
- [`check.set()`](https://tokami.github.io/iamse/reference/check.set.md)
  : check.set
- [`def.hcr.conscat()`](https://tokami.github.io/iamse/reference/def.hcr.conscat.md)
  : Define a constant-catch harvest control rule
- [`def.hcr.index()`](https://tokami.github.io/iamse/reference/def.hcr.index.md)
  : Define an index-based harvest control rule
- [`def.hcr.pseudo()`](https://tokami.github.io/iamse/reference/def.hcr.pseudo.md)
  : Define harvest control rule with pseudo assessment
- [`def.hcr.ref()`](https://tokami.github.io/iamse/reference/def.hcr.ref.md)
  : def.hcr.ref
- [`def.hcr.sam()`](https://tokami.github.io/iamse/reference/def.hcr.sam.md)
  : Define harvest control rule for SAM
- [`def.hcr.spict()`](https://tokami.github.io/iamse/reference/def.hcr.spict.md)
  : Define a SPiCT-based harvest control rule
- [`est.cons.mets()`](https://tokami.github.io/iamse/reference/est.cons.mets.md)
  : est.cons.mets
- [`est.depletion()`](https://tokami.github.io/iamse/reference/est.depletion.md)
  : Estimate depletion under alternative fishing mortalities
- [`est.metrics()`](https://tokami.github.io/iamse/reference/est.metrics.md)
  : est.metrics
- [`est.productivity()`](https://tokami.github.io/iamse/reference/est.productivity.md)
  : Estimate productivity curve
- [`est.productivity.stochastic()`](https://tokami.github.io/iamse/reference/est.productivity.stochastic.md)
  : Estimate stochastic productivity relationship
- [`est.ref.levels()`](https://tokami.github.io/iamse/reference/est.ref.levels.md)
  : Estimate deterministic reference points
- [`est.ref.levels.stochastic()`](https://tokami.github.io/iamse/reference/est.ref.levels.stochastic.md)
  : Estimate stochastic reference points
- [`est.tac()`](https://tokami.github.io/iamse/reference/est.tac.md) :
  est.tac
- [`fpat()`](https://tokami.github.io/iamse/reference/fpat.md) : fpat
- [`gen.noise()`](https://tokami.github.io/iamse/reference/gen.noise.md)
  : gen.noise
- [`get.converged()`](https://tokami.github.io/iamse/reference/get.converged.md)
  : genConvs
- [`get.errs()`](https://tokami.github.io/iamse/reference/get.errs.md) :
  get.errs
- [`get.f()`](https://tokami.github.io/iamse/reference/get.f.md) : Solve
  for fishing mortality corresponding to a target TAC
- [`get.leslie.r()`](https://tokami.github.io/iamse/reference/get.leslie.r.md)
  : get.leslie.r
- [`get.ssb0()`](https://tokami.github.io/iamse/reference/get.ssb0.md) :
  Calculate spawning stock biomass at F = 0 (SSB0)
- [`getM()`](https://tokami.github.io/iamse/reference/getM.md) : getM
- [`getMat()`](https://tokami.github.io/iamse/reference/getMat.md) :
  getMat
- [`getMsel()`](https://tokami.github.io/iamse/reference/getMsel.md) :
  getMsel
- [`getSel()`](https://tokami.github.io/iamse/reference/getSel.md) :
  getSel
- [`gettacs()`](https://tokami.github.io/iamse/reference/gettacs.md) :
  gettacs
- [`initdistR()`](https://tokami.github.io/iamse/reference/initdistR.md)
  : initdistR
- [`initpop()`](https://tokami.github.io/iamse/reference/initpop.md) :
  initpop
- [`muconv()`](https://tokami.github.io/iamse/reference/muconv.md) :
  muconv
- [`obsmod()`](https://tokami.github.io/iamse/reference/obsmod.md) :
  obsmod
- [`plotiamse.b()`](https://tokami.github.io/iamse/reference/plotiamse.b.md)
  : Plot biomass time series from an IAMSE MSE
- [`plotiamse.cw()`](https://tokami.github.io/iamse/reference/plotiamse.cw.md)
  : Plot catch time series from an IAMSE MSE
- [`plotiamse.f()`](https://tokami.github.io/iamse/reference/plotiamse.f.md)
  : Plot fishing mortality time series from an IAMSE MSE
- [`plotiamse.prod()`](https://tokami.github.io/iamse/reference/plotiamse.prod.md)
  : plotiamse.prod
- [`plotiamse.quant()`](https://tokami.github.io/iamse/reference/plotiamse.quant.md)
  : Plot IAMSE diagnostic / uncertainty quantities
- [`plotiamse.tradeoff()`](https://tokami.github.io/iamse/reference/plotiamse.tradeoff.md)
  : Plot trade-offs between IAMSE performance metrics
- [`recfunc()`](https://tokami.github.io/iamse/reference/recfunc.md) :
  recfunc
- [`run.mse()`](https://tokami.github.io/iamse/reference/run.mse.md) :
  run.mse
- [`sbr()`](https://tokami.github.io/iamse/reference/sbr.md) : sbr
- [`sdconv()`](https://tokami.github.io/iamse/reference/sdconv.md) :
  sdconv
- [`simpop`](https://tokami.github.io/iamse/reference/simpop.md) :
  Simulate an age-based population
- [`stocklist`](https://tokami.github.io/iamse/reference/stocklist.md) :
  Example stock list for iamse
- [`summary(`*`<iamse>`*`)`](https://tokami.github.io/iamse/reference/summary.iamse.md)
  : Summarise an IAMSE object

# Articles

### All vignettes

- [Introduction to
  iamse](https://tokami.github.io/iamse/articles/iamse.md):

  Learn how to get started with the basics of iamse.

- [MSE with iamse](https://tokami.github.io/iamse/articles/tutorial.md):
